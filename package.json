{
  "name": "rapidx-cloudflare-worker",
  "version": "1.0.0",
  "main": "dist/worker.js",
  "scripts": {
    "deploy": "wrangler publish",
    "build": "esbuild worker.js --bundle --outdir=dist"
  },
  "devDependencies": {
    "esbuild": "^0.14.0"
  },
  "dependencies": {}
}
